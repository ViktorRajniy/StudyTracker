<UserControl x:Class="StudyTracker.View.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudyTracker.View"
			 xmlns:vms="clr-namespace:StudyTracker.ViewModel.ViewModels"
			 d:DataContext="{d:DesignInstance Type=vms:MenuViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Menu>
			<MenuItem Header="File">
				<MenuItem Header="Add exercise..." Command="{Binding AddExercise}"/>
			</MenuItem>
		</Menu>
	</Grid>
</UserControl>
