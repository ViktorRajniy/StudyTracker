<UserControl x:Class="StudyTracker.View.Backlog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudyTracker.View"
             mc:Ignorable="d" 
			 xmlns:vms="clr-namespace:StudyTracker.ViewModel.ViewModels" 
			 d:DataContext="{d:DesignInstance Type=vms:BacklogViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<StackPanel>
			<TreeView x:Name="BackList" ItemsSource="{Binding Exercises}" >
				<TreeView.ItemTemplate>
					<HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
						<StackPanel>
							<TextBlock x:Name="Title" Text="{Binding Title}"/>
							<TextBlock Text="{Binding Description}"/>
						</StackPanel>
					</HierarchicalDataTemplate>
				</TreeView.ItemTemplate>
			</TreeView>
			<Button Command="{Binding AddExercise}" CommandParameter="{Binding ElementName=BackList, Path=SelectedItem}">Add exercise</Button>
			<Button Command="{Binding EditExercise}" CommandParameter="{Binding ElementName=BackList, Path=SelectedItem}">Edit exercise</Button>
		</StackPanel>
	</Grid>
</UserControl>
